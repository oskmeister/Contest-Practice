#include <cmath>
#include <cstdlib>
#include <cstdio>
#include <iostream>
#include <map>
#include <queue>
#include <set>
#include <string>
#include <bitset>
#include <algorithm>
#include <cstring>
#include <sstream>
#include <stack>

using namespace std;

//Utility definitions
#define rep(i, a, b) for(int i = (a); i < int(b); ++i)
#define rrep(i, a, b) for(int i = (a)-1; i >= (b); --i)
#define trav(it, v) for(typeof((v).begin()) it = (v).begin(); it != (v).end(); ++it)
#define readInt(a) scanf("%d", &a)
#define readInts(a, c) rep(i, 0, c)  scanf("%d", a+i)
#define readLong(a) scanf("%lld", &a)
#define readLongs(a, c) rep(i, 0, c) scanf("%lld", a+i)


//Typedefs
typedef double fl;
typedef long long ll;
typedef pair<int, int> pii;
typedef vector<int> vi;

void solve(){

}

int main(){
  int T;
  readInt(T);
  while(T--) solve();
}
